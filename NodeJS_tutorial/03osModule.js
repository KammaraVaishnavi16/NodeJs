// os modle is a built-in module in nodejs
//to import it
const os = require("os");

//which shows the free memory
console.log(os.freemem());
//which shows the home directory
console.log(os.homedir());
//which shows my computer host name
console.log(os.hostname());
//platform name
console.log(os.platform());
//released on
console.log(os.release());
//total memory
console.log(os.totalmem());
